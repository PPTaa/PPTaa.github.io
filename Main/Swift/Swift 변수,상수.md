# Swift 변수,상수

````swift
import swift

// 상수 (차후에 변경불가)
let 이름: 타입 = 값

// 변수 (차후에 변경가능)
var 이름: 타입 = 값

// 값의 타입이 명확하면 타입은 생략가능
let 이름 = 값
var 이름 = 값

// 나중에 값을 할당하려고 하면 타입을 명시해야함
// 상수 변수 둘다 가능
// 할당되기 전의 변수/상수는 사용할 수 없음
var sum: int
var A: int = 10
var B: int = 20
sum = A+B


````

